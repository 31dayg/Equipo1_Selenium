package youtube.pageobjects;

import org.openqa.selenium.WebDriver;

public class YoutubeResultsPageObject extends BasePageObject {
    public YoutubeResultsPageObject(WebDriver driver, String baseURL) {
        super(driver, baseURL);
    }

    // SE VAN A DEFINIR LOS ELEMENTOS DE LOS RESULTADOS DE LA BUSQUEDA POR HEADER
    // Y LAS CATEGORIAS DE LEFT MENU.
}
